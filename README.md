# LifeCompass AI

Профессиональная платформа для психологической диагностики и профориентации. Приложение использует стандартизированные методики (RIASEC, Big Five) и ИИ-аналитику для формирования персональных рекомендаций.

## 🛠 Технический стек

*   **Frontend:** React 18, Vite
*   **Стилизация:** Tailwind CSS (с поддержкой Dark Mode)
*   **Маршрутизация:** React Router Dom v6
*   **Визуализация:** Recharts (для радарных диаграмм и графиков)
*   **AI Integration:** Google Gemini API (через сервисный слой)
*   **Иконки:** Google Material Symbols

## 📂 Структура проекта

*   `components/` — Переиспользуемые UI компоненты (Sidebar, Layout, Charts).
*   `pages/` — Основные экраны приложения (Dashboard, Assessment, Results).
*   `services/` — Бизнес-логика и данные.
    *   `assessmentData.ts` — Реестр всех тестов, вопросов и вариантов ответов.
    *   `geminiService.ts` — Сервис для взаимодействия с AI (симуляция или реальный API).
*   `types.ts` — TypeScript интерфейсы для строгой типизации данных.

## 🚀 Основной функционал

1.  **Dashboard**: Обзор доступных тестов и история прохождений.
2.  **Assessment Engine**: Динамический движок квизов, поддерживающий разные типы вопросов (шкала Лайкерта, частотность).
3.  **AI Analysis**: Генерация текстовых инсайтов на основе набора ответов.
4.  **Privacy Focus**: Данные обрабатываются локально или передаются в обезличенном виде (только ID вопроса + Значение ответа).

## ➕ Как добавить новый тест

Все данные тестов хранятся в `services/assessmentData.ts`. Чтобы добавить новую методику:

1.  Откройте `services/assessmentData.ts`.
2.  Добавьте новый объект в `assessmentsDatabase` с уникальным ID.
3.  Определите структуру:
    *   `id`: Уникальный идентификатор (например, 'stress-test').
    *   `title`, `description`: Метаданные для карточки в Dashboard.
    *   `questions`: Массив вопросов.
4.  В `pages/Dashboard.tsx` добавьте карточку нового теста в массив `assessmentsUI`, указав тот же `id`.

Пример структуры теста:
```typescript
"new-test-id": {
  id: "new-test-id",
  title: "Название теста",
  questions: [
    { 
      id: 1, 
      text: "Текст вопроса", 
      category: "Категория для анализа", 
      options: standardOptions 
    }
  ]
}
```

## 🔒 Безопасность и Приватность (Закон РК)

Приложение спроектировано с учетом требований закона о защите персональных данных:
1.  **Анонимизация:** ИИ не получает ФИО, email или другие прямые идентификаторы.
2.  **Локальность:** Персональные данные профиля хранятся только на клиенте (в текущей демо-версии) или в защищенной БД.
3.  **Трансграничная передача:** В модель отправляются только векторы ответов.